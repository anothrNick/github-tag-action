{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "act - test run-tests",
      "command": "act",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "args": [
        "--rebuild",
        "--job",
        "run-tests",
        "--env-file",
        ".act/.env",
      ],
      "type": "shell",
    }
  ]
}